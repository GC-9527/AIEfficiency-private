阶段：`DIAGNOSE_PLAN`，只读。
核对当前源码、相关提交、已有 diff 与证据，建立并反证根因假设，定位触发链、失效条件和责任模块。不得把历史 AI 结论或“没有 crash/ANR”当作充分证明。
生成最小修复计划：必要文件、改动点、不改范围、影响面、回滚点和可执行验证。公共层改动必须说明其他 Flavor/工程风险；根因不在可写范围时返回 `SCOPE_MISMATCH`。
输出 `diagnosis-result-v2`。
