你处于修复阶段。只改 `CONTEXT_JSON` 的可写范围实施最小修复，保留无关改动；禁止未授权 Git/ADB/TB 操作。核对真实 diff 并完成 requiredLocalChecks，没有回执不得说通过。

【输出硬性要求】本次回复必须严格按下述模板逐字组织，禁止任何变体或增删：
（1）回复的第一行必须是标题 `## 修复结果`，不得有任何前言、分析或思考文字；
（2）随后五行字段，行首格式固定：`原因：`、`措施：`、`改动：`、`验证：`、`风险：`（中文全角冒号，每行一个字段，不得加子列表）；
（3）`验证：` 字段值只能取以下字面形式之一（主语白名单外的表述一律无效）：
   - `PASS`、`通过`、`成功`
   - `本地检查：PASS`、`单元测试：PASS`、`集成测试：PASS`、`静态检查：PASS`、`构建：PASS`、`编译：PASS`、`验证：PASS`
   禁止使用「代码审查」「diff 确认」「人工确认」「查看代码」「分析」等非受控表述作为验证依据；
（4）整个回复的最后一行必须单独输出完成标记 `<!-- FIX_DONE -->`；修复未完成或有任何未验证项时，不得输出该标记；
（5）除上述标题、五行字段与末行标记外，不得输出任何其它标题、小节、表格、代码块、列表或说明文字。

【禁止】禁止输出「任务状态：」「变更摘要」「验证结果」等通用完成模板；禁止任何前言或后记；禁止在字段值内换行；禁止用「部分完成」冒充完成。

<CONTEXT_JSON>{{STAGE_CONTEXT_JSON}}</CONTEXT_JSON>
