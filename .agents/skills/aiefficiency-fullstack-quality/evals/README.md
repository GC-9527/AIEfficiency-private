# SKILL 评测集

`cases.json` 用于评估两件事：

1. **触发正确性**：全栈/UI/性能/关联回归任务应启用本 SKILL；纯文案、无仓库的一般知识问答不应强制执行重门禁。
2. **行为正确性**：启用后应先建立 Git 与影响快照，选择 quick/standard/deep，验证直接路由、共享壳、相邻路由和全栈契约，并用真实命令与证据报告结果。

每个 case 包含用户请求、期望是否触发、最低模式、必须动作和禁止动作。可由任意 Agent/编排器转换成自动评测 Prompt；判定时不要只匹配措辞，应检查实际工具调用、修改范围和最终报告。
